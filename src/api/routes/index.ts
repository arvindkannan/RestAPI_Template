//import express, { Router } from "express";
/*routes can be added if any more*/
import sampleModule from "./sampleModule";

//const routes: Router = express.Router();
//routes.use("/api", sampleModule);

export default [...sampleModule];